# SUPERSEDED (Phase 8 Helm conversion) — this ConfigMap is now generated by
# the order-platform Helm chart (see helm/order-platform/templates/configmap.yaml).
# Kept here for reference only. Do NOT kubectl apply this file.
apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-service-config
  namespace: order-platform
data:
  SPRING_PROFILES_ACTIVE: "k8s"
  JAVA_TOOL_OPTIONS: "-Djava.net.preferIPv4Stack=true"
